<h1>Editing Experiment</h1>

<%= render 'form' %>

<%= link_to 'Show', project_phase_experiment_path(@project.id, @phase.id, @experiment) %> |
<%= link_to 'Back to phase', project_phase_path(@project.id, @phase.id) %>
